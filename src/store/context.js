 // Not sure if we'll need to end up using this.
 
 import {createContext} from "react";

 // Empty context object that we can global store state in
 const Context = createContext({});

 export default Context;